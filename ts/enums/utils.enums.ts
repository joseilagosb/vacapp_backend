export enum Environment {
  Development = "development",
  Production = "production",
  Test = "test",
}

export enum DayType {
  Weekday = "weekday",
  Weekend = "weekend",
}

export enum DayPeriod {
  Morning = "morning",
  Afternoon = "afternoon",
  Evening = "evening",
}
